/*
 * JW Control build compatibility forwarding header.
 *
 * jwcollabselection.cpp is generated into the build tree by the Google Drive
 * workspace guard. Sources generated outside sources/ no longer get the
 * original source file directory as an implicit quoted-include search path.
 * Keep the collaboration unit portable by forwarding to the canonical QET
 * header without changing upstream source layout.
 */
#pragma once

#include "sources/qetdiagrameditor.h"
